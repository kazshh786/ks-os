import{render,screen}from'@testing-library/react';import{MemoryRouter,Route,Routes}from 'react-router';import{vi,test,expect}from'vitest';import PublicFormCompletionPage,{PublicFormSuccessPage}from'../../pages/PublicFormCompletionPage.js';
test('public form renders branding and does not expose internal ids',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue({ok:true,json:async()=>({data:{salon:{name:'Test Salon',primaryColor:'#111111'},form:{title:'Consultation',description:'Before treatment',acknowledgementText:'I confirm the details are accurate.',schema:{fields:[{id:'11111111-1111-4111-8111-111111111111',type:'SHORT_TEXT',label:'Notes',required:true}]}},expiresAt:'2026-08-01T00:00:00Z'}})}));render(<MemoryRouter initialEntries={['/forms/complete/tokenvalue123456789012345678901234567890123']}><Routes><Route path="/forms/complete/:token" element={<PublicFormCompletionPage/>}/></Routes></MemoryRouter>);expect(await screen.findByText('Test Salon')).toBeInTheDocument();expect(screen.getByLabelText(/Notes/)).toBeInTheDocument();expect(screen.queryByText(/tenantId|clientId|assignmentId/)).not.toBeInTheDocument();});
test('success state is explicit and safe',()=>{render(<PublicFormSuccessPage/>);expect(screen.getByText('Form submitted')).toBeInTheDocument();});
