import{useState}from'react';import{getDataProvider}from'../../data/data-provider.js';
export default function TeamInvitePage(){const[name,setName]=useState('');const[email,setEmail]=useState('');const[msg,setMsg]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();try{await getDataProvider().createTeamInvitation({name,email});setMsg('If this address can be invited, secure setup instructions have been sent.');}catch(e){setMsg(e instanceof Error&&e.message==='TEAM_INVITATION_ALREADY_EXISTS'?'A pending invitation already exists.':'The invitation could not be sent.');}};return<div className="max-w-xl space-y-5"><h1 className="text-2xl font-black">Invite staff member</h1><p className="text-sm text-slate-500">The role is fixed to staff. KS OS never sends or stores passwords.</p><form onSubmit={submit} className="space-y-4 rounded-2xl border bg-white p-5"><label className="block font-bold">Name<input required value={name} onChange={e=>setName(e.target.value)} className="mt-1 w-full rounded-lg border p-3"/></label><label className="block font-bold">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-1 w-full rounded-lg border p-3"/></label><button className="rounded-xl bg-slate-950 px-5 py-2 font-bold text-white">Send secure invitation</button>{msg&&<p role="status">{msg}</p>}</form></div>}
