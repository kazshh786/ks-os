import React,{memo,useMemo}from'react';import type{FormField,FormSchemaJson}from'@ks-os/contracts';import{formState}from'./form-engine.js';
interface Props{schema:FormSchemaJson;answers:Record<string,unknown>;onChange:(key:string,value:unknown)=>void;page?:number;errors?:Record<string,string>;language?:string;readOnly?:boolean}
const inputClass='mt-2 w-full rounded-xl border border-slate-300 bg-white px-3 py-2.5 font-normal outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200';
function FieldView({field,value,onChange,error,required,language,readOnly}:{field:FormField;value:unknown;onChange:(v:unknown)=>void;error?:string;required:boolean;language:string;readOnly:boolean}){const key=field.key||field.id,translation=field.translations?.[language],label=translation?.label||field.label,description=translation?.description||field.description||field.helpText;const common={id:`field-${key}`,'aria-invalid':!!error,'aria-describedby':description||error?`${key}-details`:undefined,required,disabled:readOnly||field.readOnly};if(field.type==='HEADING')return<h2 className="col-span-full text-xl font-black">{label}</h2>;if(field.type==='INFORMATION')return<div className="col-span-full rounded-xl bg-slate-50 p-4 text-sm">{label}</div>;if(field.type==='DIVIDER')return<hr className="col-span-full my-2"/>;if(field.type==='HIDDEN'||field.type==='CALCULATED')return null;let control:React.ReactNode;if(['LONG_TEXT','ADDRESS'].includes(field.type))control=<textarea {...common} value={String(value??'')} onChange={e=>onChange(e.target.value)} className={inputClass} rows={4}/>;else if(['YES_NO','TOGGLE','CONSENT_CHECKBOX','TERMS_ACCEPTANCE'].includes(field.type))control=<input {...common} type="checkbox" checked={value===true} onChange={e=>onChange(e.target.checked)} className="ml-3 h-5 w-5"/>;else if(['SINGLE_CHOICE','SELECT'].includes(field.type))control=<select {...common} value={String(value??'')} onChange={e=>onChange(e.target.value)} className={inputClass}><option value="">Select…</option>{field.options?.map(o=><option key={o.id} value={o.id}>{o.label}</option>)}</select>;else if(field.type==='MULTIPLE_CHOICE')control=<fieldset className="mt-2 space-y-2">{field.options?.map(o=><label key={o.id} className="flex gap-2 font-normal"><input type="checkbox" checked={Array.isArray(value)&&value.includes(o.id)} onChange={e=>{const old=Array.isArray(value)?value as string[]:[];onChange(e.target.checked?[...old,o.id]:old.filter(x=>x!==o.id))}}/>{o.label}</label>)}</fieldset>;else if(field.type==='FILE_UPLOAD')control=<div className="mt-2 rounded-xl border border-dashed p-4 text-sm text-slate-500">Private upload becomes available after the form is assigned.</div>;else if(field.type==='SIGNATURE')control=<input {...common} value={String(value??'')} onChange={e=>onChange(e.target.value)} placeholder="Type your full name as your signature" className={inputClass}/>;else control=<input {...common} type={field.type==='EMAIL'?'email':field.type==='PHONE'?'tel':field.type==='NUMBER'||field.type==='RATING'||field.type==='SCALE'?'number':field.type==='DATE'?'date':field.type==='TIME'?'time':field.type==='DATETIME'?'datetime-local':field.type==='WEBSITE'?'url':'text'} value={String(value??'')} onChange={e=>onChange(['NUMBER','RATING','SCALE'].includes(field.type)?e.target.valueAsNumber:e.target.value)} placeholder={translation?.placeholder||field.placeholder} className={inputClass}/>;return<label className={`block font-bold ${field.width==='100'?'col-span-full':field.width==='50'?'md:col-span-1':'md:col-span-1'}`}>{label}{required&&<span aria-hidden className="text-red-600"> *</span>}{description&&<span id={`${key}-details`} className="mt-1 block text-sm font-normal text-slate-500">{description}{error&&<span role="alert" className="block text-red-700">{error}</span>}</span>}{control}</label>}
export const FormRenderer=memo(function FormRenderer({schema,answers,onChange,page=0,errors={},language='en-GB',readOnly=false}:Props){const state=useMemo(()=>formState(schema,answers),[schema,answers]);const activePage=schema.pages[page];const fields=schema.fields.filter(f=>(!activePage||!f.pageId||f.pageId===activePage.id)&&state.get(f.key||f.id)?.visible);return<section aria-label={activePage?.title||'Form questions'}><div className="grid gap-5 md:grid-cols-2">{fields.map(field=>{const key=field.key||field.id,s=state.get(key)!;return<FieldView key={field.id} field={field} value={answers[key]} onChange={v=>onChange(key,v)} error={errors[key]} required={s.required} language={language} readOnly={readOnly}/>})}</div></section>});
