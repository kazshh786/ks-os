import{useEffect,useState}from'react';import{useParams}from'react-router-dom';import{getDataProvider}from'../data/data-provider.js';
export default function FormVersionPage(){const{formId,versionId}=useParams();const[v,setV]=useState<any>();useEffect(()=>{if(formId&&versionId)getDataProvider().getFormVersion(formId,versionId).then(setV);},[formId,versionId]);if(!v)return<div>Loading immutable version…</div>;return<div className="space-y-5"><div><span className="rounded-full bg-emerald-100 px-2 py-1 text-xs font-bold">Published · immutable</span><h1 className="mt-3 text-2xl font-black">{v.titleSnapshot}</h1><p>{v.descriptionSnapshot}</p></div><div className="rounded-2xl border bg-white p-5 space-y-4">{v.schemaJson.fields.map((f:any)=><div key={f.id}><p className="font-bold">{f.label}{f.required?' *':''}</p><p className="text-sm text-slate-500">{f.helpText}</p></div>)}</div><div className="rounded-xl bg-slate-100 p-4">{v.acknowledgementText}</div></div>}
