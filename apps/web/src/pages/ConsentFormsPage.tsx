import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { FileText, Plus, ShieldAlert } from 'lucide-react';
import { getDataProvider } from '../data/data-provider.js';
import { useAuth } from '../auth/index.js';

export default function ConsentFormsPage(){const{role}=useAuth();const[forms,setForms]=useState<any[]>([]);const[error,setError]=useState('');useEffect(()=>{getDataProvider().listForms().then(setForms).catch(()=>setError('Consent forms could not be loaded. No mock data has been substituted.'));},[]);return <div className="space-y-6"><div className="flex items-center justify-between"><div><h1 className="text-2xl font-black text-slate-950">Consent forms</h1><p className="text-sm text-slate-500">Versioned templates and secure client acknowledgements.</p></div>{role==='owner'&&<Link to="/app/forms/new" className="flex items-center gap-2 rounded-xl bg-slate-950 px-4 py-2 text-sm font-bold text-white"><Plus className="h-4 w-4"/>Create draft</Link>}</div><div className="rounded-xl border border-amber-200 bg-amber-50 p-3 text-sm text-amber-900 flex gap-2"><ShieldAlert className="h-5 w-5 shrink-0"/>Responses may contain sensitive information. Share completion links only with the intended client.</div>{error&&<div role="alert" className="rounded-xl bg-red-50 p-4 text-red-700">{error}</div>}<div className="grid gap-4 md:grid-cols-2">{forms.map(form=><Link key={form.id} to={`/app/forms/${form.id}`} className="rounded-2xl border bg-white p-5 hover:border-slate-400"><div className="flex justify-between"><FileText className="h-5 w-5"/><span className="rounded-full bg-slate-100 px-2 py-1 text-xs font-bold">{form.status}</span></div><h2 className="mt-4 font-bold">{form.title}</h2><p className="mt-1 text-sm text-slate-500">Version {form.latestVersion||'—'} · {form.assignmentCount||0} assignments · {form.submissionCount||0} submissions</p></Link>)}{!error&&forms.length===0&&<div className="rounded-2xl border border-dashed p-10 text-center text-slate-500">No form templates yet.</div>}</div></div>}
