import test from'node:test';import assert from'node:assert/strict';import{CreateTeamInvitationRequestSchema,UpdateBookingChannelScheduleRequestSchema,UpdateStaffProfileRequestSchema,UpdateStaffScheduleRequestSchema,UpdateStaffServicesRequestSchema}from'@ks-os/contracts';
test('team invitation fixes role server-side and rejects tenant or role input',()=>{assert.deepEqual(CreateTeamInvitationRequestSchema.parse({email:' STAFF@Example.com ',name:'Test Staff'}),{email:'STAFF@Example.com',name:'Test Staff'});assert.throws(()=>CreateTeamInvitationRequestSchema.parse({email:'staff@example.com',name:'Test',role:'owner'}));assert.throws(()=>CreateTeamInvitationRequestSchema.parse({email:'staff@example.com',name:'Test',tenantId:crypto.randomUUID()}));});
test('profile contract cannot mutate role, tenant, id or email',()=>{for(const key of['role','tenantId','id','email'])assert.throws(()=>UpdateStaffProfileRequestSchema.parse({[key]:'owner'}));});
test('service and schedule contracts reject duplicates and invalid times',()=>{const id=crypto.randomUUID();assert.throws(()=>UpdateStaffServicesRequestSchema.parse({serviceIds:[id,id]}));assert.throws(()=>UpdateStaffScheduleRequestSchema.parse({schedule:[{dayOfWeek:1,enabled:true,startTime:'18:00',endTime:'09:00'}]}));assert.throws(()=>UpdateBookingChannelScheduleRequestSchema.parse({channel:'remote',schedule:[]}));assert.doesNotThrow(()=>UpdateBookingChannelScheduleRequestSchema.parse({channel:'mobile',schedule:[{dayOfWeek:1,enabled:true,startTime:'09:00',endTime:'17:00'}]}));});
